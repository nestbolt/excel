/**
 * Marker concern — auto-size all columns to fit their content.
 *
 * Implement this interface and set `shouldAutoSize = true` to enable.
 */
export interface ShouldAutoSize {
  readonly shouldAutoSize: true;
}
